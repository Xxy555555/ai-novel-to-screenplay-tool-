/**
 * 三阶段管线层（骨架占位）。
 *
 * <p>业务实现时在此填充：ChapterSplitter（章节切分）、AnalyzeStage（理解）、
 * ComposeStage（生成）、ValidateStage（质检）、QualityReporter（质量报告）、
 * PipelineOrchestrator（编排 + 维护角色圣经 StoryState + 发 SSE 事件）。
 */
package com.scriptforge.pipeline;
